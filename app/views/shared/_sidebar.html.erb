<div class="well sidebar-nav">
  <h3>Categories</h3>
  <ul class="nav nav-list">
    <li class="nav-header"> <%= link_to "All Categories", categories_path %> </li>
    <% Category.all.each do |category| %>
			<li><%= link_to category.name, category_path(category) %></li>
    <% end %>
  </ul>
</div>
